# CDK / Node
node_modules/
cdk.out/
# Account-specific context cache (AZ/AMI lookups) — keep out so the pilot stays
# reproducible across any AWS account.
cdk.context.json
dist/
*.js
!jest.config.js
*.d.ts
*.tsbuildinfo
coverage/
.cdk.staging/

# Python (FastAPI service)
services/api/.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.mypy_cache/
.ruff_cache/

# SPA
spa/node_modules/
spa/dist/
spa/.env
spa/*.local

# Env / secrets
.env
.env.*
!.env.example
*.pem

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Deployment scratch
outputs*.json
!cdk.json
