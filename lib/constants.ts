/** Kubernetes namespace for the application workloads (FastAPI + vLLM). */
export const APP_NAMESPACE = 'llama-pilot';

/** Kubernetes namespace for observability tooling. */
export const OBSERVABILITY_NAMESPACE = 'observability';
